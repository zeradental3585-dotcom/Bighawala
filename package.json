{
  "name": "bighawala",
  "private": true,
  "version": "1.0.0",
  "description": "BighaWala.com static site build tooling (Tailwind CSS compile-only, no other build step).",
  "scripts": {
    "build:css": "tailwindcss -i ./css/tailwind-input.css -o ./css/app.min.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "3.4.13"
  }
}
